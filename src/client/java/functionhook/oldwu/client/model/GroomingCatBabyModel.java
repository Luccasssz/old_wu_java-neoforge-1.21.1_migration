package functionhook.oldwu.client.model;

import net.minecraft.client.model.geom.ModelPart;

/** Baby grooming model shares the source grooming geometry and renderer scaling. */
public class GroomingCatBabyModel extends GroomingCatModel {
    public GroomingCatBabyModel(ModelPart root) {
        super(root);
    }
}
